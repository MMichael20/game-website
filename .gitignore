# dependencies / build output
node_modules
dist
test-results
playwright-report

# local tooling / agent dirs (never commit — .claude/worktrees are embedded repos)
.claude/
.codex/
.playwright-mcp/
*.log

# stray screenshot / capture artifacts at repo root + the assets dump
/*.png
/*.jpg
/*.jpeg
assets/
