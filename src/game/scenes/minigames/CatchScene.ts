// src/game/scenes/minigames/CatchScene.ts — stub, implemented in Chunk 4
import Phaser from 'phaser';

export class CatchScene extends Phaser.Scene {
  constructor() {
    super({ key: 'CatchScene' });
  }

  create(): void {
    // TODO: implement in Chunk 4
  }
}
