// src/game/scenes/minigames/QuizScene.ts — stub, implemented in Chunk 4
import Phaser from 'phaser';

export class QuizScene extends Phaser.Scene {
  constructor() {
    super({ key: 'QuizScene' });
  }

  create(): void {
    // TODO: implement in Chunk 4
  }
}
