// src/game/scenes/WorldScene.ts — stub, implemented in Chunk 3
import Phaser from 'phaser';

export class WorldScene extends Phaser.Scene {
  constructor() {
    super({ key: 'WorldScene' });
  }

  create(): void {
    // TODO: implement in Chunk 3
  }
}
