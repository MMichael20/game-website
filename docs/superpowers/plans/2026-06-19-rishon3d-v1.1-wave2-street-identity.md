# Rishon3D V1.1 — Wave 2: Street Identity (plan)

Spec: `docs/superpowers/specs/2026-06-18-rishon3d-v1.1-scale-city-identity-design.md`
Branch: `worktree-3d-spike` (do NOT merge/deploy). Work dir: `rishon3d/`.

Goal: roads currently look like a repeated parking-lot grid. Add visual hierarchy +
identity WITHOUT changing the road layout/network. Deterministic per-road variation so
not every road is identical. Keep blocky/voxel style, instanced/merged geometry, flat
draw-call budget. (Crosswalks + double-yellow already shipped in the detail pass.)

File-ownership (disjoint -> parallel-safe):

| Task | Owns (edits) |
|------|--------------|
| W2-A Road markings | `src/world/roads.ts`, `test/roads.test.ts` |
| W2-B Street furniture | `src/world/streetFurniture.ts` (NEW), `src/game/World.ts`, `test/streetFurniture.test.ts` (NEW) |

`roads.ts` (A) and `streetFurniture.ts`+`World.ts` (B) are disjoint. Both read `map.roads`
(read-only) for placement. Neither edits `rishonMap.ts` authored data — street identity is
derived procedurally + deterministically from the existing road network.

## W2-A — Road markings & surface identity (`roads.ts`)
Add to `makeRoadNetwork` (pure helpers + merged/instanced geometry, deterministic):
- **Stop lines**: a thick white bar across each approach lane just outside the crosswalk
  at major intersections (where a horizontal and vertical road cross). Pure
  `stopLineRects(road, intersections)` or intersection-based helper.
- **Lane arrows**: simple blocky white arrow glyphs (straight / left / right) painted on
  the asphalt approaching major intersections on the wider/core roads only. Deterministic
  choice per approach. A small merged geometry or instanced quads with an arrow texture
  (pure-data, like the sidewalk tile) — keep it node-testable.
- **Parking bays**: dashed white bay outlines along a deterministic subset of non-core
  roads (every Nth road by id hash), on the sidewalk-adjacent edge. Pairs with the
  existing parked cars.
- **Medians / refuges (optional, if clean)**: a short raised grass/concrete island at the
  centre of the longest core arterials, or a pedestrian refuge in the crosswalk — only if
  it does not fight the lane lines.
- **Curb ramps (optional, if clean)**: a small bevel/ramp block at intersection corners.
- Keep all existing roads tests green; add tests for new pure helpers (placement counts,
  determinism, core-vs-non-core gating, arrow pattern data sanity).

## W2-B — Structural street furniture (`streetFurniture.ts` NEW + `World.ts`)
New deterministic, instanced/merged props derived from `map.roads`:
- **Traffic lights**: a dark pole + a 3-light signal head (red/amber/green emissive
  blocks) at the corners of MAJOR intersections only (core origin + the few arterial/
  district junctions). Cap the count (e.g. major intersections only) for perf. Pure
  `majorIntersections(roads)` helper (find horizontal x vertical crossings, dedup, keep
  the high-degree ones) so placement is testable.
- **Road signs**: simple blocky sign posts (thin pole + small colored sign panel) along a
  deterministic subset of roadsides.
- **Bus stop + taxi stand**: 1-2 placed near the core — a small shelter (roof + post +
  bench) for the bus stop and a marked curb + sign for the taxi stand. These double as
  future gameplay anchors (car pickup), so give them stable ids/positions.
- Provide `makeStreetFurniture(map)` returning a Group (or several instanced meshes) and
  wire it into `World.ts` next to the existing prop instancers.
- Tests: `majorIntersections` determinism + count; geometry builders return non-empty
  merged geometry; placement is deterministic and within the ground bounds.

## Verification (orchestrator, after A/B)
1. `npm test` (run with `--no-file-parallelism` on Windows to avoid the Vite temp-cache
   race) — all green.
2. `npm run build` (tsc + vite) clean.
3. `npm run test:smoke` green; no console errors.
4. Playwright at 1600x900: on-foot at the core intersection (stop lines + lane arrows +
   traffic lights visible) and a driving view; confirm roads now read with hierarchy and
   identity, not a uniform grid.
5. Bounded follow-up tweaks in the owning file if a screenshot shows a miss; re-verify.

Stop at a verified Wave-2 commit. Do not merge/deploy. Then proceed to Wave 3.
