# Rishon3D V1.1 — Wave 1: Global Scale Pass + Camera (plan)

Spec: `docs/superpowers/specs/2026-06-18-rishon3d-v1.1-scale-city-identity-design.md`
Branch: `worktree-3d-spike` (do NOT merge/deploy). Work dir: `rishon3d/`.

File-ownership (disjoint -> parallel-safe):

| Task | Owns (edits) |
|------|--------------|
| W1-A UI scale | `src/ui/Minimap.ts`, `src/ui/Phone.ts`, `src/ui/Hud.ts`, `src/ui/Menu.ts`, `src/main.ts` (hint string only) |
| W1-B Prop scale | `src/world/props.ts` (streetlight only) |
| W1-C Camera | `src/core/FollowCamera.ts`, `src/core/cameraMath.ts`, `src/game/Game.ts`, `src/core/Engine.ts` |

## W1-A — UI scale (game-UI, not landing page)
- Minimap: `SIZE`/inline `180px` -> ~138px desktop via `clamp(116px, 12vw, 144px)`; keep
  canvas backing store at the rendered px (crisp). Update `minimapMath` callers only if the
  size constant is passed through (keep math pure/tests green).
- Phone: `.r3d-phone` 300x600 -> `width:clamp(220px,68vw,248px); height:auto;
  max-height:72vh; aspect-ratio:1/2` (or equivalent); icons `.r3d-icon` 54px -> 42px;
  scale car glyph + fonts proportionally.
- HUD: `.r3d-prompt` 18px -> 14px, smaller padding, `top:62%` -> ~78%; `.r3d-speedo`
  22px -> 17px.
- Bottom hint: replace the long `main.ts` controls string + `.r3d-hint` single line with
  compact **control chips** (small key+label pills). Keep `hud.setHint` working (or add a
  `setChips`/equivalent and update the one caller in `main.ts`).
- Menu: `.r3d-title` 54px -> 40px, `.r3d-btn` 20px -> 16px, tighter `.r3d-card` padding.
- Acceptance: no tests reference these styles; `npm test` stays green; screenshots show
  smaller UI. If `Hud`/`Menu` have no unit tests, none required (DOM/CSS).

## W1-B — Prop scale (streetlights)
- `makeStreetLight`: lantern box 0.5^3 -> ~0.34^3; pole `CylinderGeometry(0.08,0.1,3.4,8)`
  -> slightly thinner/shorter (e.g. `0.06,0.08,3.0`); keep lamp `y` consistent with the new
  pole top; keep emissive lantern + `World.ts` point-light budget untouched.
- Keep `props.test.ts` green (adjust only if it asserts streetlight dims).

## W1-C — Camera / framing
- `Game.ts` `setTarget`: on-foot `10` -> 12.5 (lookHeight ~1.6 keep), car `11` -> 14
  (lookHeight ~1.5, maybe raise camera for intersections).
- `FollowCamera.ts`: default `distance` and `MAX_DIST` (18) raised if needed for the new
  car distance; keep zoom clamps sane.
- `Engine.ts`: keep FOV 60 (or 58) — change only if it clearly helps.
- `cameraMath.ts`: pure offset math — only touch if a framing tweak needs it; keep
  `cameraMath.test.ts` green.
- Acceptance: `npm test` green (camera math tests still pass); driving/on-foot views read
  with more world context, less foreground dominance.

## Verification (orchestrator, after A/B/C)
1. `npm test` (vitest, all) green.
2. `npm run build` (tsc + vite) clean.
3. `npm run test:smoke` (playwright) green.
4. Playwright at **1600x900**: title menu, on-foot city, phone home, pause menu. Compare to
   pre-wave captures; confirm smaller UI + less zoomed-in scene; no console errors.
5. Bounded follow-up tweaks in the owning file if a screenshot shows a miss; re-verify.

Stop at a verified Wave-1 commit. Do not merge/deploy. Then proceed to Wave 2.
